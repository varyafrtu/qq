import { Inter } from 'next/font/google';
import { Lusitana } from 'next/font/google';

export const inter = Inter({
  weight: ["400", "700"],
  subsets: ['latin'], // Add subsets for Inter
});

export const lusitana = Lusitana({
  weight: ["400", "700"],
  subsets: ['latin'], // Add subsets for Lusitana
});